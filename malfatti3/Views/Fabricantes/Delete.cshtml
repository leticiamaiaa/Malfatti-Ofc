@model malfatti.Models.Fabricante

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/App_Themes/Fabricante/Delete.css" />
    <link rel="stylesheet" type="text/css" href="delete1.css" />
    <title>Malfatti</title>
</head>
<body>
    <header>
        <h1>Dados do FABRICANTE a ser removido!</h1>
    </header>
    <main>
        @using (Html.BeginForm("Delete", "Fabricantes", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <h3>Nome:</h3>
            @Html.DisplayFor(model => model.Nome)

            <input id="submit" type="submit" value="Remover Fabricante" />

            @Html.ActionLink("Retornar para a listagem de FABRICANTES", "Index", null, new { @class = "btn btn-warning" })}
    </main>
</body>
</html>
